apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitlab-arm-pv-claim
spec:
  storageClassName: gitlab-arm-pv-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 9Gi
